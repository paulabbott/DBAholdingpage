import React from 'react';
import HoldingPage from '../components/holdingPage';

var HoldingPageContainer = React.createClass({

    render: function() {
        return (
            <HoldingPage/>
        );
    }

});

export default HoldingPageContainer;